[package]
name = "outer"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
inner.workspace = true
# Add a vendored dependency to test the proxy with external crates.
itoa = "1"
