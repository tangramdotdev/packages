{
  "dependencies": {
    "@types/bun": "^1.3.6",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0"
  },
  "scripts": {
    "auto": "bun scripts/package_automation.ts $@",
    "check": "oxlint scripts && bun scripts/package_automation.ts --check",
    "format": "oxfmt --write scripts && bun scripts/package_automation.ts --format",
    "test": "bun scripts/package_automation.ts --test"
  }
}
