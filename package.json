{
  "dependencies": {
    "@biomejs/biome": "^2.2.7",
    "@types/bun": "^1.3.0"
  },
  "scripts": {
    "auto": "bun scripts/package_automation.ts $@",
    "check": "biome check scripts && bun scripts/package_automation.ts --check",
    "format": "biome check --write scripts && bun scripts/package_automation.ts --format",
    "test": "bun scripts/package_automation.ts --test"
  }
}
